import { Address } from './address';

export class Contact {
  firstName?: string;
  lastName?: string;
  job?: string;
  mobilePhone?: string;
  groundPhone?: string;
  mailBox?: string;
  contactAddress?: Address;
}
